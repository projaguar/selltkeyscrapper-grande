# 알려진 문제 / 인수인계 (2026-08-07 운영 중단 시점)

> 이 문서는 **운영을 중단하고 폐기하는 시점**의 상태 기록이다.
> 이 코드를 다시 쓰거나 다른 곳에 이식하려는 사람이 **먼저 읽어야 할 것**을 담았다.
>
> - 마지막 배포본: `fe27725`
> - 관련 문서: `REDESIGN.md`(재설계 근거·불변식·모델 검증), `DEPLOY.md`(운영 런북)
> - 검증 모델: `bun run model/check.ts`

---

## 0. 요약 — 이 코드를 다시 쓴다면

| 우선 | 항목 | 조치 |
|---|---|---|
| **필수** | 페인트 프로브(rAF) 대규모 오탐 (§1) | **관측 전용으로 낮추거나 제거**하고 시작할 것 |
| 권장 | 동시 브라우저 수 (§2) | 16GB 맥에서는 **13** 이 적정. 15 는 오히려 처리량 절반 |
| 참고 | 남은 리스크 (§3) | 미구현/미검증 항목 목록 |

§1 을 그대로 두고 기동하면 **정상 브라우저가 계속 재활용되어 가동 슬롯이 줄어든다.**

---

## 1. [치명] 페인트 경로 프로브(rAF)의 대규모 오탐 — 미수정

### 무엇을 하려던 기능인가

조작자가 **검게 보이는 브라우저 창**을 발견해 조사한 결과, 실제로 렌더러의 JS 스레드는 살아있는데
컴포지터(페인트 경로)만 멈춘 슬롯이 있었다. 문제는 **어떤 지표로도 잡히지 않았다**는 점이다.

당시 실측 (`serial 1247` = `k1f8g2yo` = `scrapper-1783152210476-2`, 우리 그룹 슬롯):

| 검사 | 결과 |
|---|---|
| CDP url/title | `https://www.naver.com/` / `NAVER` — 정상 응답 |
| `page.evaluate` (기존 keepalive) | **통과** ← 그래서 건강 판정 |
| `requestAnimationFrame` | **TIMEOUT** |
| `Page.captureScreenshot` | **타임아웃** (정상 슬롯은 139KB) |
| `crawler-get-health` | `operational 15/15`, `error 0` — **전부 초록** |

크롤 자체는 `__PRELOADED_STATE__` 를 `evaluate` 로 읽으므로 계속 동작했고, 네비게이션 중앙값도
315ms 로 정상이었다. 즉 **"살아있는데 제 기능을 못 하는" 차원이 관측 표면에서 통째로 빠져 있었다.**

조작자가 주소창을 클릭해 수동 새로고침하자 복구됐고, CDP 로 재확인하니 **포트 65338 그대로**
(브라우저 재시작 없음)인 채 `rAF: PAINT_OK`, 스크린샷 81KB 정상으로 돌아왔다.
→ **리로드만으로 컴포지터가 복구된다**는 것까지 확인했다.

이 근거로 `keepalive` 에 rAF 프로브를 넣고, 실패 시 강제 리로드 → 실패 시 재활용하도록 구현했다
(`642ab76`, 오탐 방어로 연속 2회 조건 추가 `fe27725`).

### 무엇이 잘못됐나

프로덕션 투입 후 **대규모 오탐**이 발생했다. 중단 직전 누적:

```
paintStuck(정지 확정) : 182
paintRecovered(복구)  :   1
리로드 후에도 정지     : 181
rAF 1회차 무응답       : 208
가동 슬롯             : 10/13  (정상일 때 13/13)
```

**오탐 판정 근거 3가지:**

1. **13개 프로필 전부에 균등 분포** (각 12~18회). 실제 컴포지터 wedge 는 특정 슬롯에서
   산발적으로 발생하지 정확히 고르게 나오지 않는다.
   ```
   18회 scrapper-1784097880527-3    14회 scrapper-1783152210476-2
   15회 scrapper-1784097888246-9    14회 scrapper-1783152008059-2
   15회 scrapper-1784097885655-7    13회 scrapper-1784097881791-4
   15회 scrapper-1783152213001-4    13회 scrapper-1784097877999-1
   15회 scrapper-1783152207936-0    12회 scrapper-1784097886937-8
   14회 scrapper-1783152215578-6    12회 scrapper-1783152214291-5
                                    12회 scrapper-1783152211734-3
   ```
2. **복구율 1/182.** 진짜 wedge 라면 리로드로 대부분 복구됐어야 한다
   (실제 wedge 였던 `1247` 은 리로드로 복구됐다).
3. 181건이 `error` 로 전환 → 재활용 반복 → **가동 슬롯이 10/13 으로 감소**.

### 근본 원인

`rAF` 프로브의 **2.5초 타임아웃이 부하 상태에서 너무 빡빡했다.**
13개 브라우저가 동시에 크롤하며 loadavg 11~16 인 상황에서는, 정상 브라우저도 2.5초 안에
프레임이 스케줄되지 않는다. Chromium 은 가려진/백그라운드 창의 rAF 를 적극적으로 스로틀한다.

**검증이 부실했던 지점:** 배포 전에 "15개 슬롯 전부 rAF 통과"를 확인했으나, 그 테스트는
**외부에서 슬롯당 1회씩 순차 실행**한 것이었다. 실제 운영은 13개가 동시에 크롤하고
keepalive 가 주기적으로 도는 조건이라 부하 프로파일이 전혀 달랐다.
`PAINT_FAIL_STREAK_TO_ACT = 2` 완충도 무력했다 — 부하는 일시적이 아니라 지속적이므로
연속 2회도 쉽게 충족된다.

### 다시 쓴다면

- **즉시**: `src/lib/crawler/CrawlerBrowser.ts` 의 `keepalive()` 에서 리로드·`error` 전환을
  제거하고 **카운터/로그만** 남긴다(관측 전용). 그것만으로 무증상 블랙아웃의 **발생 여부**는 보인다.
- **탐지기를 개선한다면** 판별력이 실측으로 확인된 신호는 `Page.captureScreenshot` 타임아웃이다
  (정상 139KB vs wedge 타임아웃). 비싸므로 rAF 로 1차 선별 후 스크린샷으로 확증하는 2단 구조가 맞다.
- **fleet 단위 안전장치 필수**: 동시에 과반이 stuck 으로 잡히면 그것은 환경 요인(부하/스로틀)이지
  개별 wedge 가 아니다. 이 경우 조치를 건너뛰고 경고만 남겨야 한다. 지금 코드에는 이 가드가 없다.
- **검증은 반드시 실부하에서**: 목표 동시성으로 크롤을 돌리는 상태에서 오탐률을 측정할 것.

관련 코드: `src/lib/crawler/CrawlerBrowser.ts` (`keepalive`, `paintAlive`, `PAINT_*` 상수),
`src/lib/crawler.ts` (`health.cumulative.paintStuck/paintRecovered`).

---

## 2. 동시 브라우저 수 — 16GB 맥에서는 13 이 적정

15 로 올렸다가 **처리량이 절반 이하로 떨어져** 13 으로 되돌렸다. 실측 비교:

| | 13개 | 15개 |
|---|---|---|
| 처리량 | **41~44건/분** | **19건/분** |
| loadavg (10코어) | 11~16 | 20~30 |
| 압박 정지(인테이크 중단) | **0회** | **12회** |
| swap 사용 | 2.4~4.9GB | 5.9GB |
| macOS 압박 레벨 | 1 (정상) | 2 (경고) |

원인: 15개에서는 loadavg 가 압박 임계(코어수×2.5 = 25) 부근에서 계속 교차해
**인테이크가 단속적으로 멈춘다.** 브라우저를 늘려서 얻는 것보다 멈춰서 잃는 것이 크다.

수용 제어 상수는 이 실측에 맞춰 보정돼 있다(`src/server.ts`):
`(총 16384MiB − 예약 3072MiB) ÷ 850MiB = 15`.
15 를 **허용**하되 20 은 차단하는 값이다. 13 으로 강제하려면 DB 설정
`crawlProfileCount = 13` 을 쓰거나 `SCRAPPER_PER_BROWSER_MB` 를 올린다.

> 참고: 20 으로 뒀을 때 16GB 맥이 멈춰 강제 리부팅 3회를 했다(여유 59MB, loadavg 89).
> 이것이 수용 제어를 넣은 계기다.

---

## 3. 남은 리스크 / 미검증 항목

### 3.1 모델 검증의 한계

`model/check.ts` 는 **무작위 스케줄 탐색**(구성별 2400~4800 스케줄)이지 전수 증명이 아니다.
반증이 없다는 것이 부재의 증명은 아니다. 또한 모델이 다루는 범위는 **프록시·프로필 소유권과
수명주기**뿐이다. 다음은 모델 대상이 아니며 실측으로만 확인했다:

- AdsPower 큐의 head-of-line 차단
- 서킷 브레이커 임계값
- 로그 상한
- 메모리 클램프
- **페인트 경로 생존** ← §1 의 사고가 정확히 이 사각지대에서 났다

### 3.2 관측 표면의 사각지대

`crawler-get-health` 는 소유권·유한성·진행성은 덮지만, **"살아있는데 품질이 나쁜"** 차원은
여전히 약하다. §1 의 페인트가 그 예였고, 다음도 지표화되어 있지 않다:

- 슬롯별 수집 성공률 / 빈 수집 비율
- 슬롯별 네비게이션 지연 추세(현재는 로그를 사후 집계해야 보인다)
- 프록시별 품질(차단률·지연)

### 3.3 상세 로그 기본 비활성

로그 폭증(0.7GB/일 → 365일 250GB, 디스크 고갈 약 230일)을 막기 위해 `[Navigate]`,
`[TaskQueue]` 추적 로그를 `SCRAPPER_VERBOSE=1` 뒤로 내렸다(`28f65b3`).
**문제 조사 시에는 이 플래그를 켜야 한다.** 끈 상태에서는 태스크 단위 추적이 어렵다.

또한 `[Navigate]` 중 `goBack` 타이밍 한 줄은 격하 치환에서 누락되어 그대로 출력된다
(용량 영향은 미미해 방치했다).

### 3.4 로그 트림 방식

launchd 가 stdout/stderr fd 를 소유하므로 파일 교체가 불가해, **앞부분을 버리고 최근 20MB 만
남기는 트림**으로 상한을 지킨다(`src/lib/log-budget.ts`, 기본 상한 200MB).
트림 창(읽기→쓰기) 동안의 기록은 유실될 수 있다 — 로그로는 허용 가능하다고 판단했다.

### 3.5 정지 후 상태

정지해도 프록시 lease 는 `in_use` 로 남는다. **다음 부팅 시 단일 인스턴스 락을 잡은 프로세스가
자동 회수**하므로 수동 SQL 리셋은 불필요하다(`reclaimAllAtBoot`). 예전에는 수동 리셋이 필요했다.

---

## 4. 해결된 문제 (재발 여부 판단 기준)

폐기 시점 기준으로 **해결이 실측 확인된** 항목. 코드를 재사용할 때 이 지표들이 다시 나빠지면
회귀를 의심할 것.

| 문제 | 해결 근거 |
|---|---|
| 프록시 in_use 누수 (사이클당 +1) | **117회 IP 회전 / 45시간 누적 가동에서 고아 lease 회수 0** |
| 한 IP 뒤 두 브라우저 | 소유권 검증 반납 + 원자적 획득. 소유권 불일치 0건 |
| 사라진 프로필 무한 재시도 | 타입드 에러(`ProfileGoneError`) → 재생성 라우팅 |
| 무한 재시도 스핀(15시간, 로그 350MB) | 백오프·캡·보류·재활. 보류 0건 |
| 정지 불가 좀비 크롤러 | 세대 토큰 + 실제 드레인 + 무조건 `stopCrawler` |
| 메모리 고갈로 머신 정지 | 수용 제어 + 런타임 부하 가드 |
| 로그 무한 증가 | 상세로그 off + 상한 트림 → 717MB/일 → 9MB/일 |
| 무인증 RPC 로 타 앱 프로필 손상 | `SessionManager` + `session-*` RPC 4개 삭제 |
| `addProxy` 단건 추가 100% 실패 | 컬럼 6 / 값 5 바인딩 오류 수정 |

**최종 누적**: 총 완료 511,707건, launchd 크래시 재시작 0회.

---

## 5. 폐기 시점 처리 내역 (2026-08-07)

- 크롤러 서버·caffeinate·launchd-run 종료, 포트 4478 해제, pid 락 제거
- `scrapper` 그룹 브라우저 15개 **개별 종료**(`stop-all` 미사용 — 타 앱 브라우저 보호)
- `~/Library/LaunchAgents/com.selltkey.scrapper.plist` **삭제** (부팅 자동실행 제거)
  - 백업: `~/scrapper-decommission-20260807-150109/`
  - **보존한 것**: `com.selltkey.ads-broker.plist`, `com.selltkey.prowler.plist`,
    `com.selltkey.prowler-watchdog.plist` — 타 앱 소관이라 건드리지 않았다
- 빌드 산출물·실행파일 제거 (전부 `.gitignore` 대상, git 추적 0건):
  `release/` 1.0G, `node_modules/` 831M, `out/` 892K, `dist/` 292K → 저장소 1.8G → 4.2M
- **남아있는 것**: DB(`~/Library/Application Support/selltkeyscrapper-grande/`, 프록시 100개·설정),
  로그(`~/Library/Logs/selltkey-scrapper.*.log`), AdsPower `scrapper` 그룹 프로필 15개

### 복구 방법

```bash
bun install
bun run build
# 자동실행이 필요하면 백업된 plist 를 되돌리고 bootstrap
cp ~/scrapper-decommission-*/com.selltkey.scrapper.plist ~/Library/LaunchAgents/
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.selltkey.scrapper.plist
```

**단, 재가동 전에 반드시 §1 을 처리할 것.**
